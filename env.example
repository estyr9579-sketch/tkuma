# Supabase (Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...   # server only, never exposed

# Admin identification (server only). A profile whose phone OR email matches gets the admin role.
ADMIN_PHONE=0506616981
ADMIN_EMAIL=ab0548463077@gmail.com

# Public site URL (used for SEO / sitemap / auth redirects)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: email notification on new lead (Resend)
RESEND_API_KEY=
LEAD_NOTIFY_EMAIL=

# Optional: Google Search Console HTML-tag verification code (the content="..." value only)
GOOGLE_SITE_VERIFICATION=
